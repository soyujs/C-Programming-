#include <stdio.h>

int main()
{
    int a; // VARIABLE DECLERATION
    a = 6; // VARIABLE INITALIZATION

    // TO MAKE IT MORE EASIER WE CAN DO:
    int b = 2;
    printf("The output of this program is %d", a);
    printf("The output of this program is %d", b);

    // FOR A DECIMAL WE USE FLOAT:
    float c;
    c = 3.4;

    // TO MAKE IT MORE EASIER WE CAC DO:
    float d = 4.5;
    printf("The output of the program id %d", c);
    printf("The output of the program is %d", d);

    // FOR CHARACTERS:
    char e = 'a';

    // TO TAKE INPUT:
    int a;
    scanf("%d", &a);
    printf("The value of a is %d" ,a);
    return 0;
}
